type User {
  id: ID! @id
  email: String @unique
  name: String!
  workshops: [Workshop!]!
}

type Workshop {
  id: ID! @id
  title: String!
  published: Boolean! @default(value: false)
  author: User @relation(link: INLINE)
  preview: String!
}
