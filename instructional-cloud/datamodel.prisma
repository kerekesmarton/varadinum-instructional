type User {
  id: ID! @id
  email: String @unique
  name: String!
  workshops: [Workshop!]!
}

type Workshop {
  id: ID! @id
  title: String!
  author: User @relation(link: INLINE)
  preview: String!
}
